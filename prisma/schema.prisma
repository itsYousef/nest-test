generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model profile {
  id    BigInt  @id @default(autoincrement())
  name  String?
  email String?
  phone String?
  age   Int?
}
